# Harness Desktop 1.0.61

上一稳定版是不可变的 `v1.0.60`。现行官方 Harness Runtime 与依赖图继续固定 `0.1.2-alpha.5`，本次不升级官方依赖。本说明描述候选源码；发布完成状态以唯一发布器核验的公开资产及签名更新源为准。

## 本次更新

- **纳入 PR #47**：运行时从安全动态端口范围选择独立回环端口，避免 Chromium 拒绝端口导致工作台打不开；团队 SSE 首次握手有界等待，停滞连接会释放。没有放宽 Chromium 安全策略。
- **手机连接与同步**：WSS 旧连接的错误/关闭回调不能破坏新代连接；工作区清单 HTTP/WS 鉴权统一、取消贯穿准备与请求阶段，关闭过程保留错误吸收并有界结束。
- **手机恢复与交互**：真实连接提示、恢复失败及手动重试、附件预览焦点和可访问性同步 Android/iOS；不因失败自动重发用户消息。
- **手机控制安全**：命令和结果继续绑定确切设备连接及回执身份，拒绝旧连接结果，不新增权限或绕过确认。

## 范围与状态

整合精确 PR #47 头 `28c35a933aa626d5a4e7ffaf1206ac15b9e1748f` 与本轮已验证的 14 个手机相关文件；不纳入维护树其他浏览器、离屏、原生预览或团队扩展改动。官方 Harness 依赖继续固定 `0.1.2-alpha.5`，集成版本与自有插件同步 `1.0.61`，Android versionCode 为 `1006100`，iOS build 为 `10061`。

本次按用户要求不追加测试或现场复现。候选整合不等于现场外网故障已解决：旧安装版尚无本轮修复，也缺少当次脱敏错误栈和实际传输类型。发布器内置源码/安全门禁、云端构建、Android 长期签名、Apple 模拟器、正式 Windows 制品与双云资产核验仍是发布前提，未以局部回归替代。

## 获取更新

正式发布完成后，通过应用更新中心或[永久最新版下载页](https://github.com/baiyuscc13724-max/deepseek-harness-desktop/releases/latest)获取。GitHub 到 CNB 只走统一发布器云到云镜像；不重建旧 Tag、不覆盖旧资产、不上传本地二进制。iPhone/iPad 继续使用 Safari 工作台入口，不发布未签名 IPA。
