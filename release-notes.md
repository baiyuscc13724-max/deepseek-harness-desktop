# Harness Desktop 1.0.49

## Agent Teams 成果直接可见

- 成员完成任务后，匹配该成员运行的最终文本结果会持久化到对应任务，用户现在可以直接在已完成任务卡片、团队看板、侧栏和任务详情中查看成果，不再只有负责人能够收到成员报告。
- 结果只接受最终助手消息中的文本块并限制为最多 12,000 个字符；原始工具事件、非文本内容、内部协调消息和会话载荷不会投影到用户界面。
- 已完成任务重开、停止或重新进入待处理状态时会清除旧结果，避免过期成果被误认为当前任务输出；旧版团队存储会从 schema 1–3 加法迁移到 schema 4。

## P2P 协商期间保持 WSS 可用

- Android 检测到原生 P2P 能力后会立即开放已有的端到端加密 WSS/SOCKS 后备线路，同时在后台继续 WebRTC 协商，不再让外网工作台等待 offer 才获得可用通道。
- v2 迁移顺序、旧流关闭、会话密钥、重放保护和直连失败回退边界保持不变；此次修复只改善可用线路的启动时序，不扩大 relay、设备或模型权限。

## Computer Use 指示与结构化优先路由

- 移除控制遮罩的无限透明度动画与整页 backdrop filter，重复活动状态同步改为幂等，导航后的注入样式通过原子替换恢复，降低指示器闪烁和页面视觉抖动。
- Computer Use 的系统提示和工具说明明确为最后后备：文件、仓库、网页和手机任务必须先使用专用结构化数据通道；网页在 CDP/DOM 可用时不能退回截图坐标控制。
- 可信宿主授权卡、Esc 停止、外部窗口目标绑定以及浏览器密码、账户、验证码、支付和银行敏感输入限制均未放宽。

## 版本、构建与完整性

- 桌面、14 个随包插件、Android 与 iOS/iPadOS 源码统一到 `1.0.49`；Android `versionCode=1004900`，iOS build code 为 `10049`。
- `v1.0.48..v1.0.49` 的产品改动完整包含 PR #25、#26 和 #31；PR #27 仍是未合并的文档 PR，只有其独立签名 Preview feed promotion 记录位于主分支，不作为本次产品源码改动。
- 已发布 `v1.0.48` 的 Tag、精确 18 项资产、签名 Android APK 与 stable 组件保持不可变。v1.0.49 继续由统一发布器绑定精确 main SHA，通过 GitHub Actions 全平台构建与签名，再由 CNB 云到云镜像，最后提升三个 stable feed。
