# Harness Desktop 1.0.38

## 本次更新

- 修复运行中的旧版 Harness Desktop 导致覆盖升级失败并返回 Windows 错误码 5：安装器改用 Inno Setup `CloseApplications=force`，桌面进程收到 Windows Restart Manager 的会话结束通知后会进入真实退出路径、优雅停止运行时，并且不会在升级后自动重启旧应用。
- 本版继续以修复问题和降低运行、发布负担为主：修复启动失败、右侧浏览器来回闪烁，以及 Git 浏览器授权重复弹码、授权后不刷新。
- 继续兼容 v1.0.29 起的旧版客户端更新方式和 Chromium 重定向差异；HTTPS 来源、签名清单与 SHA-256 校验均未放宽。
- 整理设置、模型、会话菜单和“已安排的任务”等既有页面，修复搜索标签、顶栏、菜单与统计文字重叠；账户额度和主/子模型设置保持可见，归档与已安排入口不再占据主导航。
- 增加独立、持久化的壁纸库页：一键导入后保存为本地卡片，即使 Wallpaper Engine、Steam 或原始来源关闭仍可使用；从任何主题双击即可应用并回到主界面，自定义背景文字会按背景对比度适配。
- 新用户的本地记忆默认开启；已经明确关闭记忆的旧用户不会被重新开启，查看、删除、敏感内容过滤和关闭入口保持不变。
- 修复代理团队本机、局域网和跨网络建队，并采用扁平扩员：成员只能为已领取且确有独立工作流的任务提出结构化申请，由固定负责人批准为同级可见成员，不增加隐藏“组员”层。

## macOS

- macOS 无 Apple Developer 会员时的显式无签名契约、Intel/Apple Silicon 双架构 DMG/ZIP、`安装.command` 一键安装助手和云端结构自检流程完全不变。
- 未签名包不等同于 Developer ID 签名、Apple 公证或 Gatekeeper 验收；推荐打开 DMG 后使用其中的 `安装.command`。

## 发布与完整性

- 正式发布绑定唯一不可变 `v1.0.38` Tag；本文件准备阶段不会提前修改 stable feed 或已发布资产清单。
- 在普通客户端检测到更新前，统一可恢复发布器必须完成 Windows 安装版/便携版、打包后自检，以及真实下载、安装、更新、回滚和卸载验证。
- GitHub 跨平台云构建、签名 Android、签名组件、精确 18 项清单与 GitHub→CNB 云镜像全部成功后，才最后提升 stable feed；中断后从原子状态续跑，不重复搬运已验证的大文件。
- 所有公开资产仍需提供并验证 SHA-256，生产组件与稳定源继续强制 Ed25519 签名。
