# Harness Desktop 1.0.13

- 窗口拖动改为动态识别空白区域：顶部和正文空白处都能拖动，按钮、链接、文字、输入框和菜单会自动避让，不再因新增顶部功能反复出现“拖不动”或“按钮点不到”。
- 子代理入口和列表恢复完整点击区域；点击子代理会进入官方实时会话详情页，查看运行内容、上下文与轨迹。
- 缓存指标改为优先显示“最近一步缓存读取”，并在说明中区分热请求、前缀复用、含首次冷启动的累计值，以及“提供方未报告”，避免把正常冷启动误判成缓存失效。
- 修复跟随主模型时删除 `harness-desktop-routing` 导致历史会话无法恢复的问题；兼容预设会持续保留并随主模型更新，用户自建预设不会被覆盖或误设为桌面路由。
- 桌面模型路由在配置、状态和生成预设都未变化时不再重复写入，减少启动时无意义的配置抖动。
- 官方工作区里的本机文件、文件夹和项目路径现在可以直接打开；右键可复制路径或在资源管理器中定位。
- 修复顶部桌宠卡片必须点关闭按钮的问题，点击卡片外区域即可自动收起。
- Harness 运行依赖改为随应用主包归档，安装阶段不再逐个释放上万份小文件；首次启动会建立带版本隔离的本地运行缓存。
- 保留 v1.0.12 的子代理运行标识、更新内容提醒和插件市场中文摘要。
