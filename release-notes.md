# Harness Desktop 1.0.60

本次仅修复代理团队的范围管理、正常等待和停止后清理，不包含已经取消的浏览器、离屏或原生预览任务。官方 Harness 依赖继续固定在 `0.1.2-alpha.5`，不作新的运行时升级。

上一稳定版是不可变的 `v1.0.59`。本说明描述候选源码；发布是否完成以唯一发布器核验的公开资产与签名更新源为准。

## 三项修复

- **范围提醒**：显示首次成员开工时的内部任务数与之后新增的任务数，提醒对照原始目标及用户最新要求收口。任务数量不是用户需求数量，也不是授权证明；历史时间不完整时不编造基线。
- **静默等待**：`team_status.wait_for_change=true` 在当前已授权回合内订阅真实进展，而不是不断查询、生成空轮次或汇报“仍在等待”。Stop、取消及插件退出会释放订阅；没有可继续产生成果的成员时返回需处理状态。它不操作 Goal、不补发权限、不启动成员，也不替代自动接力的授权边界。
- **停止后直接清理**：精确根会话在直接用户回合内可取消已暂停任务、关闭已停止团队，不再要求先 Resume。取消仍受任务 revision、pause epoch 和审计历史约束；成员只被排空，不发送恢复或退休工作提示。排空失败仍保持暂停，遇到更新的 Stop 拒绝旧清理结果。

## 版本与验证

集成版本为 `1.0.60`；官方 Harness 依赖保持已锁定的 `0.1.2-alpha.5`。Android/iOS 的版本元数据同步更新，不包含新的移动功能。

源码回归覆盖范围计数、真实注册工具、被动订阅、取消/身份变化、暂停成员排空失败及 Stop 竞态。现有完整测试、源代码安全审查、云构建、签名、正式 Windows 制品自检与双云精确清单仍由发布门禁逐项验证，不能以局部测试代替。

## 获取更新

正式发布完成后，请通过应用更新中心或[永久最新版下载页](https://github.com/baiyuscc13724-max/deepseek-harness-desktop/releases/latest)获取。禁止重建旧 Tag、覆盖已公开资产或上传本地二进制；GitHub 到 CNB 的镜像由唯一发布器协调。
