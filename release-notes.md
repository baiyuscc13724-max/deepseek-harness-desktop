# Harness Desktop 1.0.48

## 原生 P2P 与个人 WSS 后备

- 新增桌面与 Android 原生 WebRTC DataChannel P2P 直连。个人 WSS/443 只用于信令与连接受限时的盲转发后备；P2P 协商失败不会中断现有端到端加密同步。
- P2P v2 会话密钥绑定房间、双方身份与双 nonce；数据帧继续绑定方向、peer 与 session AAD，并用 4096 包滑动窗口拒绝重放。v2 建立后拒绝旧 v1 帧、路径重绑定与会话降级。
- WSS relay 保留总连接、握手、房间、来源、帧大小、速率和背压上限，不持有端到端密钥或明文。STUN 仅接受固定 allowlist，不支持把 TURN 凭据带入客户端。
- 修复活动通道断线清理与用户关闭远程同步并发时，旧轮次仍可能启动备用适配器的资源生命周期竞态；异步 prepare/start/fallback 在提交前必须匹配当前 lifecycle revision，停止会等待在途切换收敛。

## Android、手机外观与输入体验

- 手机端新增独立于电脑端的皮肤设置。手机状态不继承桌面背景文件，主题资源只允许固定随包 allowlist；侧栏、消息区、设置页和输入区保持足够不透明与可读。
- 输入框只在文本区聚焦且原生 IME 或可视窗口确实被键盘覆盖时抬升，键盘关闭后恢复；Android Back 继续仅把任务移到后台，不终止已配对会话、代理或远程同步。
- 照片入口改为单图快速选择、可重复添加，并先把临时 URI 内容复制成页面持有的 File 再交给官方附件管线；不新增相册或录音常驻权限。
- Android 14+ 使用系统 `DETECT_SCREEN_CAPTURE` 回调在用户截屏后显示“添加刚才截图”入口；回调只通知发生过截屏，不返回 Bitmap、URI 或媒体内容，实际读取仍须用户再次打开系统 Photo Picker 选择。
- 对会话/子代理 history 的短暂 abort/5xx 增加有界重试、请求去重与最多 8 项短期缓存；流式消息 token 不触发全页面 MutationObserver 扫描。

## Agent Teams、更新与桌面交互

- Agent Teams 任务增加显式 `cancelled` 终态、失败前置投影、取消审计和一致重开；已取消前置不会被误当作普通等待，也不会静默恢复为待处理。
- 优雅成员退休会拒绝仍由成员持有的未完成任务，优雅团队关闭会拒绝任何未完成任务；强制关闭先记录取消再收敛，已完成历史保持不变。
- PR Preview 默认发现经过签名的已批准候选，并复用正常更新通知，不再加载重复悬浮卡；候选仍须用户确认才应用，退出与失败继续自动回滚。
- 修复共享更新通知识别到签名组件后仍可能沿完整安装器路径执行的问题；组件通知现在显式绑定组件暂存、健康检查与原子重启路径。
- Computer Use 永久授权只表示跨重启记住授权，软件启动时不会自动控制；只有活动的外部窗口目标显示控制指示，Esc 停止与可信宿主授权边界保持不变。

## 版本、构建与完整性

- 桌面、14 个随包插件、Android 与 iOS/iPadOS 源码统一到 `1.0.48`；Android `versionCode=1004800`，iOS build code 为 `10048`。
- 完整三段版本的签名 APK 与桌面资产共用不可变 `v1.0.48` Release；四段独立 Android 修订版仍使用 `android-v<version>`。iPhone/iPad 在没有 Apple Developer 会员时继续通过 Safari/添加到主屏幕使用实时工作台，不发布未签名 IPA。
- WebRTC Android 依赖及 BSD-3-Clause 许可随包记录在第三方声明中；正式 Android workflow 继续校验包名、版本、长期证书身份与公开 APK 字节。
- 已发布 `v1.0.47` 的 Tag、精确 18 项资产、签名 APK 与组件保持不可变。统一发布器仍只采用锁定 SHA 的 GitHub Actions 正式制品，由 CNB 云到云镜像；两云精确回读后才提升三个 stable feed。
