# Harness Desktop 1.0.34

## 本次更新

- 修复桌面启动时 Computer Use 插件注入不完整导致的 Harness 启动失败；Windows 安装器会先关闭占用 DLL 的旧实例，再完成覆盖升级。
- 修复更新下载在 Chromium 手动重定向被取消时失败的问题，继续兼容旧客户端读取方式并保留 SHA-256 校验。
- 设置页、模型页和“已安排的任务”页面完成布局整理；账户额度、子模型选择恢复可见，会话 ID 移入 Codex 风格会话菜单。
- 修复浏览器右侧工作区反复开关；本机建队、局域网自动 mTLS 和跨网络端到端邀请/批准流程恢复可用。
- 第二次 CNB stable 同步只处理元数据，不再重复搬运 18 项不可变资产；桌面、插件、Android 与 iOS/iPadOS 源码版本同步到 1.0.34。

## macOS

- macOS 的显式无签名契约、双架构 DMG/ZIP、一键安装助手及云端自检流程与 v1.0.32 完全一致，本版本不作任何改变。
- 未签名包不等同于 Developer ID 签名、Apple 公证或 Gatekeeper 验收；推荐打开 DMG 后使用其中的 `安装.command`。

## 发布与完整性

- 发布绑定唯一不可变 `v1.0.34` Tag。
- 在普通客户端检测到更新前，统一发布器会完成本地 Windows 安装版/便携版、打包后自检和真实更新下载/安装验证。
- 只有 GitHub 跨平台云构建、签名 Android、签名组件、精确 18 项清单、GitHub→CNB 云镜像全部成功后，才最后提升 stable feed。
- 所有公开资产均提供 SHA-256 校验信息，并由统一可恢复发布器记录真实结果。
