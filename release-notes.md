# Harness Desktop 1.0.24

## 新功能

- 新增 Codex 风格右栏浏览器、可见地址栏、独立持久化 Profile，以及由用户亲自完成的网站登录与站点数据管理。
- 新增按站点、按动作授权的 `browser_control`；模型只能操作当前可见页面，提交类操作逐次确认。
- 新增默认关闭的本地 SQLite 跨会话记忆与只读 `local_memory` 有限召回工具。
- 新增仅限 Harness Desktop 窗口的受限 `computer_use`，支持截图、逐次确认的点击/输入/滚动、一键停止与用户接管。
- 工作区选择改为由主窗口拥有的原生目录窗口，解决窗口难以唤起或隐藏到后台的问题。
- 新增扫描—预览—确认式存储清理，永久保护会话、附件、记忆、当前运行时和活动临时文件。

## 体积与性能

- 女仆鲸桌宠 280 帧转换为八个无损 WebP 图集，按需加载且最多保留三个已解码图集。
- 精简开发源素材、语言包及非运行时依赖内容，并新增安装包、便携版、ASAR、语言包、桌宠与仓库素材体积门禁。

## 安全与隐私

- 浏览器 Cookie 与官方 Harness 会话完全隔离；密码、Cookie、Authorization、令牌、验证码、支付和银行内容不会提供给模型。
- 本地记忆默认关闭；敏感内容默认拒绝保存，模型不能保存、修改、删除或整库读取记忆。
- Computer Use 每次启动默认关闭，只能截取和操作 Harness Desktop 自身窗口，不执行 Shell 或脚本。
- Android APK 保持 1.0.20 测试版不变，本版本不构建或修改 Android 应用。

## 验证

- 288 项桌面单元、安全、集成和制品契约测试全部通过。
- 源码 Electron 自检通过；右栏浏览器、记忆工具、Computer Use 与工作区窗口均完成隔离真实运行验证。
