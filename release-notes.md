# Harness Desktop 1.0.26

## 关键修复

- 修复安装后退出时浏览器 `webContents` 已被 Electron 释放而触发的 JavaScript 错误；浏览器显示、隐藏、重置和关闭均增加生命周期竞态保护。
- 右键菜单明确提供“复制链接地址”；窗口拖动捕获不再阻断灰色选区清除，并支持 Esc 取消选区。
- 桌面工作台锁定外层视口，避免输入框和对话框随整页向上滚动；手机端仍保留正常纵向滚动。
- Codex 配额直接使用 Harness 已登录的 OAuth 查询官方 WHAM，外部 CLI 只在无 Harness 凭据时后备；返回结果不含令牌、账户 ID 或个人信息。

## 低干扰本地记忆与缓存

- 本地记忆像 Codex 一样在后台按需使用，仅保存稳定偏好、长期指令、项目约束和可复用事实，不保存整段对话。
- 密码、API key、令牌、Cookie、Authorization、验证码、银行卡和支付银行内容继续硬拒绝或按用户设置脱敏。
- 模型自动写入只允许直接用户驱动的根会话；搜索数量、内容长度、敏感等级和总条数均有上限，并自动去重。
- 超过 7 天的应用自有 Marketplace 缓存自动维护；会话、附件、记忆、工作区、当前运行时和活动临时文件永久排除。
- 原标题栏快捷按钮移到系统托盘“数据与隐私”，保留状态查看、关闭、搜索、导出、单项删除、全部安全擦除和导出副本删除。

## 能力与界面

- 整合默认关闭的 Agent Teams 实验插件：固定负责人、可恢复队友、依赖任务、消息鉴权、并发上限和文件冲突提示。
- 子代理目录新增“当前 / 历史 / 全部”筛选，明确区分运行中、可继续和已结束记录；一次性任务结束后保留完整只读过程，不删除会话。
- Computer Use 截图改为会话级临时存储，限制单文件/总大小/数量/时效，并在停用、接管、启动和退出时清理。
- 增加官方经典、极光玻璃、空间专注和触感实体四种可逆界面模式，同时保留 92% 透明度、动态壁纸和文字保护。

## 生产组件更新

- v1.0.26 是一次完整引导安装包，内置独立 Ed25519 生产公钥并公开启用签名组件更新。
- 每个平台使用独立稳定清单；CNB 国内源优先，GitHub 全球源自动后备。
- 组件 ZIP、组件描述和顶层清单全部验签并校验 SHA-256；激活后必须通过打包自检健康确认，否则自动回滚到上一健康版本。
- Electron、原生 `.node` 模块、Bootstrap、安装器和不兼容变更继续使用完整安装包兜底。

## 平台

- Windows、macOS Intel、macOS Apple Silicon、Android 与 iOS/iPadOS 源码同步到 1.0.26。
- Android 继续使用长期正式 release 证书，工作流强制验证包名、版本、证书指纹和 APK 签名；绝不发布 debug 或未签名 APK。
- 用户未加入 Apple Developer Program，因此 iPhone/iPad 继续使用通过二维码配对的 Safari 实时工作台，不分发不可公开安装的未签名 IPA。
