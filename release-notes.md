# Harness Desktop 1.0.40

## 本次更新

- 代理团队继续位于原来的“代理团队”页，原有 Team Runtime、Broker、Memory、Scheduler、本机/局域网/跨网络接入能力不减少；新增统一工作台，在同一处查看所选团队的任务板、团队画布、流程、现有定时任务、参与者和协调 Inbox。
- 任务多时不再横向挤压卡片：看板按容器响应式显示 1/2/4 列，每列独立滚动并保留真实总数；当前阶段每个团队最多投影 200 项到 UI，旧任务仍留在运行时与历史中。
- 团队画布支持自动 Fit、10%–200% 缩放、平移、键盘操作、节点自动换行和有界关系线；自动团队启停收紧为紧凑控制，避免一个开关占据整块页面。
- Team worker 使用进程级公平准入和有界等待：8 个活跃槽、32 个全局等待、每个根最多 8 个等待、30 秒超时，并支持取消、Stop 和精确释放。压力测试覆盖 12/24 个根、250 项任务、SSE 合并和慢客户端背压；这还不是全桌面 Provider/API 统一调度池。
- 强化内嵌浏览器的用户与模型来源隔离、导航防护、请求防重放、停止和取消恢复；模型不能借点击、重定向、下载或弹窗绕过当前可见标签、来源授权和敏感操作确认。
- 修复设置主题预览不能完整滚动、说明文字裁切和 Windows 系统模式标题栏按钮对比度；壁纸库改进当前 Wallpaper Engine 项目识别、图片/视频预览、受管导入与背景可读性。
- DSH Plugin Marketplace 升级到上游 1.5.5，并使用单一可信注册所有权避免重复加载；`@deepseek-ai/cordis-plugin-group@1.0.1` 继续作为直接生产依赖并由打包自检验证。
- 多电脑阶段继续限定为安全配对、加密连通和在线状态预览；本版不宣称已经支持跨电脑任务共同编辑、离线任务同步或冲突合并。

## macOS

- macOS 无 Apple Developer 会员时的显式无签名契约、Intel/Apple Silicon 双架构 DMG/ZIP、`安装.command` 一键安装助手和云端结构自检流程完全不变。
- 未签名包不等同于 Developer ID 签名、Apple 公证或 Gatekeeper 验收；推荐打开 DMG 后使用其中的 `安装.command`。

## 发布与完整性

- Linux CI 与 Release 在运行真实 Electron 浏览器导航安全测试前，显式把 `chrome-sandbox` 配置为 `root:root`/`4755` 并复核权限；配置失败会阻断构建，不以 `--no-sandbox` 降级通过。
- 正式发布绑定唯一不可变 `v1.0.40` Tag；本文件准备阶段不会提前修改 stable feed 或已发布资产清单。
- 在普通客户端检测到更新前，统一可恢复发布器必须完成 Windows 安装版/便携版、打包后自检，以及真实下载、安装、更新、回滚和卸载验证。
- GitHub 跨平台云构建、签名 Android、签名组件、精确 18 项清单与 GitHub→CNB 云镜像全部成功后，才最后提升 stable feed；中断后从原子状态续跑，不重复搬运已验证的大文件。
- 所有公开资产仍需提供并验证 SHA-256，生产组件与稳定源继续强制 Ed25519 签名。
