# Harness Desktop 1.0.32

## 本次更新

- 新增统一右侧工作区：隔离浏览器、工作区文件、网页链接和有界文本/代码文档预览统一在可返回、可调整宽度且响应式的侧栏中打开。
- “已安排”任务页新增搜索、建议、活动/停用筛选和最近历史；任务创建、停用与重新创建仍会先进入输入框，由用户检查后手动发送。
- 修复长会话在模型返回 `CONTEXT_WINDOW_EXCEEDED` 后未自动恢复的问题：按完整工具调用边界压缩并有限重试，压缩配置无法安全建立时不再静默继续。
- 手机同步服务避开浏览器永久禁用端口，避免极少数配对链接被浏览器直接拒绝。
- 桌面、插件、Android 与 iOS/iPadOS 源码版本同步到 1.0.32；Android 继续只发布长期证书签名 APK。

## macOS

- macOS 的显式无签名契约、双架构 DMG/ZIP、一键安装助手及云端自检流程与 v1.0.31 完全一致，本版本不作任何改变。
- 未签名包不等同于 Developer ID 签名、Apple 公证或 Gatekeeper 验收；推荐打开 DMG 后使用其中的 `安装.command`。

## 发布与完整性

- 发布绑定唯一不可变 `v1.0.32` Tag。
- 在普通客户端检测到更新前，统一发布器会完成本地 Windows 安装版/便携版、打包后自检和真实更新下载/安装验证。
- 只有 GitHub 跨平台云构建、签名 Android、签名组件、精确 18 项清单、GitHub→CNB 云镜像全部成功后，才最后提升 stable feed。
- 所有公开资产均提供 SHA-256 校验信息，并由统一可恢复发布器记录真实结果。
