# Harness Desktop 1.0.23

## 修复内容

- 现在可以把包含历史图片的会话切换到只支持文本输入的模型，不再因为旧消息中的图片永久阻止模型切换。
- 请求发送前仅将历史图片替换为明确的文字占位，不修改磁盘中的原始会话记录；切回支持图片的模型后仍可继续使用原始图片。
- 工具结果中嵌套的历史图片也会递归安全处理。
- 当前输入框中尚未发送的新图片仍会阻止切换到纯文本模型，避免用户刚添加的图片被静默丢弃。
- 运行时补丁支持重复执行，并在官方运行时结构发生不兼容变化时明确失败，避免错误替换。

## 验证与发布

- 9 项定向兼容测试和 156 项完整桌面测试全部通过。
- 发布前继续执行正式构建、制品审计、打包后自检以及安装/卸载冒烟测试。
- GitHub 完成发布后，CNB Runner 会直接下载并校验制品，再由官方 `cnbcool/attachments` 插件上传到国内镜像。
- 本机不会向 CNB 传输安装版或便携版大文件。

## 安全边界

- 发布包不包含用户 API 密钥、本机代理凭据、发布凭据或手机配对数据。
- 所有下载源均通过文件大小和 SHA-256 校验，镜像异常时可自动回退 GitHub。
