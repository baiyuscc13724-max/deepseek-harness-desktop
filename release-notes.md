# Harness Desktop 1.0.19

## 这次更新

- 内置官方 DeepSeek Harness 0.1.0-rc.7，并完成桌面补丁与运行时兼容升级。
- 官方核心版本查询优先使用国内 npmmirror；不可用时自动回退 npm 官方 Registry 和 GitHub。
- 桌面更新支持 CNB 国内镜像与 GitHub 后备源，并对所有下载强制校验大小和 SHA-256。
- 新增 Provider 用量适配，并把正式安装的 DSH 数据、工作区和临时目录稳定放入安装目录的 `HarnessData`。
- 在现有 Harness Mobile APP 中加入手机控制权限向导、总开关、前台通知和“立即停止”，没有创建第二个 APP。
- Agent 可在已配对且用户授权的手机上执行固定白名单动作：读取可访问节点摘要、点击、长按、滑动、返回、主页、最近任务、非密码文本输入、打开应用/链接/设置、单次截图和系统文件选择器。
- 桌面与手机协商协议版本和能力；每条命令都有 ID、超时、有限重试、取消和明确结果回执。
- 输入文字、保存文件和清理缓存会在手机上再次确认；清理缓存无法安全识别时只提供手动引导，绝不点击“清除数据”。
- 原扫码下载、APP 内扫码配对、会话同步、实时刷新、官方界面结构和安装数据保持不变。

## 下载说明

- Windows 中文安装版：适合普通用户安装与后续更新，并保留原安装位置。
- Windows 便携版 EXE：下载后直接运行，无需安装。
- Android 通用测试版 APK：沿用 1.0.18 已安装包的现有测试签名，以支持覆盖安装并保留配对数据。

## 安全边界

- 密码、支付、银行、验证码、账户安全、清除数据、静默安装/卸载、权限绕过、任意 Shell/脚本和自动发送消息不会执行。
- 控制关闭、桌面停止、设备撤销、APP 退出或服务停止时，待执行命令立即失效。
- 发布包不包含用户 API 密钥、本机代理凭据或手机配对数据。
- 无论从 CNB 还是 GitHub 下载，Windows 安装包都必须通过发布清单中的 SHA-256 校验。
