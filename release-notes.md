# Harness Desktop 1.0.46

## 受保护的 PR 快速更新

- 新增官方同仓库 PR Preview 更新通道：PR 代码只在无密钥、只读凭据的构建任务中产出组件；默认分支上的受保护签名任务重新查询 PR、head SHA、构建 run 与官方稳定基线后，才使用独立 Ed25519 Preview 密钥生成不可变候选。
- 签名阶段不再直接修改 `latest`。候选必须先通过本机精确四资产校验、真实组件 stage/apply、进程重启健康检查、last-known-good 失败回滚和退出 Preview 后的 stable 恢复，随后由独立 Required Reviewer Environment 才能提升。
- 客户端固定 CNB 优先、GitHub 后备的双源地址，严格验证官方仓库、同仓非 fork PR、`main` 基线、单调 sequence、最长七天有效期、完整 SHA-256 与签名；未知来源、额外字段、重放、降序或签名错误全部失败关闭。
- Preview 激活复用现有内容寻址组件存储、分离 helper、健康确认和稳定指针恢复；界面只展示已验证候选，并明确提供人工应用、退出 Preview 与恢复状态。
- CNB 仅从 GitHub 不可变 Release 云到云下载并完整回读大小/SHA 后提升；handoff 绑定远端 `pr-preview` 的精确父 OID，使用普通 non-force push，拒绝并发变化、额外树内容、force/force-with-lease 或未通过本机证据的请求。

## 桌面与工作台体验

- 统一 Computer Use 授权状态、浏览器控制和跨应用策略展示；授权范围仍只能由 Harness Desktop 宿主卡上的“本次授权 / 永久授权 / 拒绝”决定，插件不能自行扩大权限。
- 右侧工作区支持经过边界校验的原生文件按钮和有界文本/代码预览；普通聊天文本继续拒绝被误判为本机路径。
- 明确的“打开右侧浏览器”与 HTTP(S) 地址指令由随包客户端能力安全消费；共享浏览器控制仍保留登录、支付、账户和凭据输入边界。
- 加强模态框焦点约束与恢复、小窗口授权卡可达性、背景可读性、主题层级、会话归档永久删除重试，以及停止生成后的跟随体验。
- Agent Teams 工作台保留项目任务详情、SSE/runtime 投影、项目自动化、业务同步、受控 Git 能力、加密完整性和安全的 optional-service 边界。

## Android、移动同步与个人中继

- 修复 Android API 26–32 对高版本 Java API 的兼容问题，覆盖配对、文件选择、前台服务、更新检查和错误路径，避免旧系统 `NoSuchMethodError`。
- 修复 WSS 中继连接后的无限 SOCKS 线程增长：每条连接使用单接收线程、固定工作池和有界队列，过载明确拒绝；本地 SOCKS/Web 代理固定绑定 `127.0.0.1`。
- 新增用户显式配置的个人 `wss://`/443 中转地址和检测、保存、清除流程；地址变化要求重新扫码，服务只盲转发端到端加密帧，不持有隧道密钥或内容。
- 提供独立 Node 中继、Caddy 与 systemd 示例，并对连接、握手、房间、来源、帧大小和速率实施有界限制。

## 版本、发布与完整性

- 桌面、13 个随包插件、Android 与 iOS/iPadOS 源码同步到 `1.0.46`，Android `versionCode=10046`。
- 已发布 `v1.0.44` 以及现有 `v1.0.45` Tag/草稿全部保持不可变；`v1.0.45` 不移动、不覆盖、不复用，完整功能版本使用新的 `v1.0.46`。
- 正式 `v1.0.46` Tag 仅在干净候选、本机源码/安全门禁、受保护 PR Preview 更新/重启/回滚、锁定 SHA 的全平台云端候选构建，以及 previous-stable→candidate Windows 安装升级验证全部成功后创建。
- 统一可恢复发布器仍是唯一发布入口：正式二进制仅由 GitHub Actions 构建，CNB 从 GitHub 云到云镜像；精确 18 项资产和三个签名 stable feed 最后提升，同版本不同摘要一律视为冲突。
